<nav class="navbar navbar-expand-lg navbar-dark bg-dark ps-5 bg-secondary bg-gradient">
  <a class="navbar-brand" href="/"> Static Site Genereator </a>

  <button
    class="navbar-toggler"
    type="button"
    data-bs-toggle="collapse"
    data-bs-target="#toggleMobileMenu"
    aria-controls="toggleMobileMenu"
    aria-lable="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse d-flex justify-content-center display-flex pe-5" id="toggleMobileMenu">
    <ul class="navbar-nav text-center">

        {% for item in menu %}
        <a class="nav-link" href="{{ item.path }}">{{ item.name }}</a>
        {% endfor %}

    </ul>
  </div>
</nav>
